
array.map((item, i) => { 
    const a = item.a

    return ( 
        <div>
            goo goo gaga
            <div>{a}</div>
        </div>
    )
})

array.map((item, i) => 
    (
        <div>
            googoo gaga
        </div>
    )
)